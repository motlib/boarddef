{% macro std(drill) %}
<g transform="translate({{ drill.center_x }}, {{ drill.center_y }})">

  <!-- Simple white drill hole -->
  <circle cx="0" cy="0"
	  r="{{ drill.radius }}" 
	  style="fill: #ffffff" />
</g>
{% endmacro %}
